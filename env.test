# Testing Environment Variables (No SSL Required)
NODE_ENV=production
PORT=5000

# Database Configuration
DATABASE_URL=postgresql://geiuser:geipassword@db:5432/gei?schema=public

# JWT Configuration
JWT_SECRET=Qw3rty!9zXcVbN7mLpOiUjK1hGfDsA4eR

# Upload Configuration
UPLOAD_DIR=/app/uploads
MAX_FILE_SIZE=15728640

# Frontend API URL (HTTP for testing)
VITE_API_URL=http://localhost:5000/api

# SMTP Configuration (update these with your actual values)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your-email@gmail.com
CONTACT_RECEIVER=contact@gei.org

# Database Password (for docker-compose)
DB_PASSWORD=geipassword

# Optional: Seed database on first run
# SEED_DATABASE=true 